using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraController : MonoBehaviour
{
   /* private Transform player;
    private Camera cam;
    [SerializeField] private Vector3 playerPositionInCameraOffset;
    [SerializeField] private Vector2 playerFollowThreshold;
    [SerializeField] private float cameraMovementDampening;

    private void Awake()
    {
        player = GameObject.Find("Player").transform;
        cam = GetComponent<Camera>();
        cam.transform.position = player.transform.position + playerPositionInCameraOffset;
    }

    private void Update()
    {
        
    }*/
}
